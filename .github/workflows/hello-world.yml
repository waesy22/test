name: hello-world
on:
  push:
    branches:
      - main
      - slave
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: my-step
        run: docker build -t waesy/server:1.0.0 -f Dockerfile .